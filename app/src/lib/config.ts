/**
 * API endpoint configuration.
 * Keep values in one place so that network layer stays simple to maintain.
 */
export const BASE_URL = "http://127.0.0.1:8000";

/** Fully-qualified endpoint for the GigaChat proxy. */
export const REQUEST_ENDPOINT = `${BASE_URL}/api/v1/request`;
